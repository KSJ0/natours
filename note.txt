npm init = to initialize package.json
npm i express

HTTP METHODS:
POST = ( Create )
GET = ( Read )
PUT/PATCH = ( put = entire updated object, patch = only updated object )
DELETE = ( Delete )


define port. for example: port = 3000;
app.listen() = start up a server and listen on the port specified by the port parameter.

define route above port. for example: app.get('/', (req, res) => {})